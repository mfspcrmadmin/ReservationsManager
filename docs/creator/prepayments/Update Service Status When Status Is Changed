Update_Service_Status_Whe as "Update Service Status When Status Is Changed"
{
	type =  form
	form = Pre_Payments_Form

	record event = on edit

	on user input of Status
	{
		actions 
		{
			custom deluge script
			(
					thisapp.CRM.bs_update_Status(input.Proforma_Associated.ID,input.ID,input.Status);
			)
		}
	}

}

